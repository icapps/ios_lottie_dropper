# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LottyDropper' do
   use_frameworks!

   pod 'ObjectiveDropboxOfficial'
   pod 'Stella', '~> 1.1.0'

  target 'LottyDropperTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
